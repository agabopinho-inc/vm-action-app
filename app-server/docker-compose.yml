version: "3.9"

services:
  nginx: 
    name: nginx
    image: atekcr.azurecr.io/nginx:${TAG}
    deploy: 
      replicas: 2
    ports: 
      - 80:80
    networks: 
      - frontend
      - backend
      
    